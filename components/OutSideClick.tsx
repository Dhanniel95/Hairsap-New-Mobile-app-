import React from "react";
import { StyleSheet, Text, View } from "react-native";

const OutSideClick = () => {
	return (
		<View>
			<Text>OutSideClick</Text>
		</View>
	);
};

export default OutSideClick;

const styles = StyleSheet.create({});
