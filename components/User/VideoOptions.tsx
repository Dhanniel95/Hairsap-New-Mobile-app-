import React from "react";
import { StyleSheet, Text, View } from "react-native";

const VideoOptions = () => {
	return (
		<View>
			<Text>VideoOptions</Text>
		</View>
	);
};

export default VideoOptions;

const styles = StyleSheet.create({});
