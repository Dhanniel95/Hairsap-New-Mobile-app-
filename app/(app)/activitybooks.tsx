import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ActivityBookings = () => {
	return (
		<View>
			<Text>ActivityBookings</Text>
		</View>
	);
};

export default ActivityBookings;

const styles = StyleSheet.create({});
